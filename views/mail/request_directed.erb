Hello sheriffs,
<%= req.subject.fullname %> (<%= req.subject.nick %>) has just requested that someone sub as sheriff on <%=
  req.day.day.strftime '%A, %B %d, %Y'
%> with the following comment:
<%= req.comment %>

If you'd like to take this request, go to:
<%= link %>

Thank you very much,
The Sheriffbot
